## 25. 低代码平台如何实现安全机制和权限控制?

**答案:**

低代码平台的安全机制涉及数据安全、权限控制、代码隔离、审计日志等多个方面。

### 1. 数据安全

```javascript
// 数据加密工具
class DataEncryptor {
  constructor(secretKey) {
    this.secretKey = secretKey;
  }
  
  // 加密数据
  encrypt(data) {
    const json = JSON.stringify(data);
    const encrypted = this.xor(json, this.secretKey);
    return btoa(encrypted);
  }
  
  // 解密数据
  decrypt(encryptedData) {
    const encrypted = atob(encryptedData);
    const json = this.xor(encrypted, this.secretKey);
    return JSON.parse(json);
  }
  
  // XOR加密
  xor(str, key) {
    let result = '';
    for (let i = 0; i < str.length; i++) {
      result += String.fromCharCode(str.charCodeAt(i) ^ key.charCodeAt(i % key.length));
    }
    return result;
  }
}

// 敏感数据脱敏
class DataMasker {
  // 脱敏手机号
  maskPhone(phone) {
    if (!phone) return phone;
    return phone.replace(/(\d{3})\d{4}(\d{4})/, '$1****$2');
  }
  
  // 脱敏邮箱
  maskEmail(email) {
    if (!email) return email;
    const [username, domain] = email.split('@');
    const maskedUsername = username.substring(0, 2) + '***';
    return maskedUsername + '@' + domain;
  }
  
  // 脱敏身份证
  maskIdCard(idCard) {
    if (!idCard) return idCard;
    return idCard.replace(/(\d{6})\d{8}(\d{4})/, '$1********$2');
  }
  
  // 脱敏银行卡
  maskBankCard(cardNo) {
    if (!cardNo) return cardNo;
    return cardNo.replace(/(\d{4})\d+(\d{4})/, '$1 **** **** $2');
  }
}
```

### 2. 代码沙箱隔离

```javascript
// 沙箱环境
class Sandbox {
  constructor() {
    this.allowedGlobals = {
      console: console,
      Math: Math,
      Date: Date,
      JSON: JSON,
      Array: Array,
      Object: Object,
      String: String,
      Number: Number,
      Boolean: Boolean
    };
    
    this.builtins = ['eval', 'Function', 'setTimeout', 'setInterval', 'fetch', 'XMLHttpRequest'];
  }
  
  // 创建隔离环境
  createContext(context = {}) {
    const sandbox = { ...this.allowedGlobals, ...context };
    
    // 禁用危险的全局对象
    this.builtins.forEach(name => {
      delete sandbox[name];
    });
    
    return sandbox;
  }
  
  // 安全执行代码
  async execute(code, context = {}) {
    const sandbox = this.createContext(context);
    
    try {
      // 使用Proxy拦截访问
      const sandboxProxy = new Proxy(sandbox, {
        get(target, prop) {
          if (prop in target) {
            return target[prop];
          }
          
          // 阻止访问原型链上的危险属性
          if (prop === 'constructor' || prop === '__proto__') {
            return undefined;
          }
          
          return undefined;
        },
        
        set(target, prop, value) {
          if (prop in target) {
            target[prop] = value;
            return true;
          }
          return false;
        }
      });
      
      // 执行代码
      const func = new Function('sandbox', `
        with(sandbox) {
          ${code}
        }
      `);
      
      return func(sandboxProxy);
    } catch (error) {
      console.error('代码执行失败:', error);
      throw new Error('代码执行被阻止: ' + error.message);
    }
  }
}

// 使用示例
const sandbox = new Sandbox();

// 安全执行
sandbox.execute(`
  console.log('Hello from sandbox');
  console.log(Math.random());
`).then(result => {
  console.log('执行结果:', result);
});

// 危险操作会被阻止
sandbox.execute(`
  fetch('https://evil.com');  // 会被阻止
`).catch(error => {
  console.error('危险操作被阻止:', error.message);
});
```

### 3. XSS防护

```javascript
// XSS防护工具
class XSSProtector {
  // 转义HTML
  escapeHtml(unsafe) {
    return unsafe
      .replace(/&/g, '&amp;')
      .replace(/</g, '&lt;')
      .replace(/>/g, '&gt;')
      .replace(/"/g, '&quot;')
      .replace(/'/g, '&#039;');
  }
  
  // 转义JS
  escapeJs(unsafe) {
    return unsafe
      .replace(/\\/g, '\\\\')
      .replace(/'/g, "\\'")
      .replace(/"/g, '\\"')
      .replace(/\n/g, '\\n')
      .replace(/\r/g, '\\r')
      .replace(/\t/g, '\\t')
      .replace(/\f/g, '\\f');
  }
  
  // 净化HTML
  sanitizeHtml(html) {
    const temp = document.createElement('div');
    temp.textContent = html;
    return temp.innerHTML;
  }
  
  // URL白名单验证
  validateUrl(url, whitelist) {
    try {
      const parsedUrl = new URL(url);
      
      return whitelist.some(allowedDomain => {
        return parsedUrl.hostname === allowedDomain ||
               parsedUrl.hostname.endsWith('.' + allowedDomain);
      });
    } catch (error) {
      return false;
    }
  }
}

// CSP头配置
const cspConfig = {
  'Content-Security-Policy': `
    default-src 'self';
    script-src 'self' 'unsafe-inline' 'unsafe-eval';
    style-src 'self' 'unsafe-inline';
    img-src 'self' data: https:;
    font-src 'self' data:;
    connect-src 'self' https://api.example.com;
    frame-src 'none';
    object-src 'none';
  `.trim()
};

// 设置CSP头
function setCSP(response) {
  Object.keys(cspConfig).forEach(header => {
    response.setHeader(header, cspConfig[header]);
  });
}
```

### 4. 审计日志

```javascript
// 审计日志系统
class AuditLogger {
  constructor() {
    this.logs = [];
    this.maxLogs = 10000;
  }
  
  // 记录日志
  log(action, context) {
    const logEntry = {
      id: this.generateId(),
      timestamp: new Date(),
      action,
      userId: context.userId,
      sessionId: context.sessionId,
      ip: context.ip,
      userAgent: context.userAgent,
      data: context.data,
      result: context.result
    };
    
    this.logs.push(logEntry);
    
    // 限制日志数量
    if (this.logs.length > this.maxLogs) {
      this.logs.shift();
    }
    
    // 异步保存到服务器
    this.saveToServer(logEntry);
  }
  
  // 生成ID
  generateId() {
    return Date.now().toString(36) + Math.random().toString(36).substr(2);
  }
  
  // 保存到服务器
  async saveToServer(logEntry) {
    try {
      await fetch('/api/audit-logs', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(logEntry)
      });
    } catch (error) {
      console.error('保存审计日志失败:', error);
    }
  }
  
  // 查询日志
  query(filters) {
    return this.logs.filter(log => {
      if (filters.action && log.action !== filters.action) return false;
      if (filters.userId && log.userId !== filters.userId) return false;
      if (filters.startTime && log.timestamp < filters.startTime) return false;
      if (filters.endTime && log.timestamp > filters.endTime) return false;
      return true;
    });
  }
  
  // 导出日志
  export(filters) {
    const logs = this.query(filters);
    const csv = this.convertToCSV(logs);
    return csv;
  }
  
  // 转换为CSV
  convertToCSV(logs) {
    const headers = ['ID', 'Timestamp', 'Action', 'User ID', 'IP', 'Data'];
    const rows = logs.map(log => [
      log.id,
      log.timestamp.toISOString(),
      log.action,
      log.userId,
      log.ip,
      JSON.stringify(log.data)
    ]);
    
    return [headers, ...rows].map(row => row.join(',')).join('\n');
  }
}

// 操作类型
const AuditActions = {
  PAGE_CREATE: 'PAGE_CREATE',
  PAGE_UPDATE: 'PAGE_UPDATE',
  PAGE_DELETE: 'PAGE_DELETE',
  PAGE_PUBLISH: 'PAGE_PUBLISH',
  COMPONENT_ADD: 'COMPONENT_ADD',
  COMPONENT_REMOVE: 'COMPONENT_REMOVE',
  PROPERTY_UPDATE: 'PROPERTY_UPDATE',
  USER_LOGIN: 'USER_LOGIN',
  USER_LOGOUT: 'USER_LOGOUT',
  PERMISSION_CHANGE: 'PERMISSION_CHANGE'
};

// 使用示例
const auditLogger = new AuditLogger();

// 记录操作
auditLogger.log(AuditActions.PAGE_CREATE, {
  userId: 'user-001',
  sessionId: 'session-123',
  ip: '192.168.1.1',
  userAgent: 'Mozilla/5.0...',
  data: {
    pageId: 'page-001',
    pageName: '示例页面'
  },
  result: 'success'
});
```

### 5. 细粒度权限控制

```javascript
// 权限模型
class PermissionModel {
  constructor() {
    this.roles = new Map();
    this.permissions = new Map();
    this.userRoles = new Map();
  }
  
  // 创建角色
  createRole(roleId, roleName) {
    this.roles.set(roleId, {
      id: roleId,
      name: roleName,
      permissions: new Set()
    });
  }
  
  // 添加权限
  addPermission(permissionId, permissionDef) {
    this.permissions.set(permissionId, {
      id: permissionId,
      resource: permissionDef.resource,
      action: permissionDef.action,
      conditions: permissionDef.conditions || []
    });
  }
  
  // 为角色分配权限
  assignPermissionToRole(roleId, permissionId) {
    const role = this.roles.get(roleId);
    if (role) {
      role.permissions.add(permissionId);
    }
  }
  
  // 为用户分配角色
  assignRoleToUser(userId, roleId) {
    if (!this.userRoles.has(userId)) {
      this.userRoles.set(userId, new Set());
    }
    this.userRoles.get(userId).add(roleId);
  }
  
  // 检查用户是否有权限
  hasPermission(userId, permissionId, context = {}) {
    // 获取用户角色
    const userRoleIds = this.userRoles.get(userId) || new Set();
    
    // 检查角色是否有权限
    for (const roleId of userRoleIds) {
      const role = this.roles.get(roleId);
      if (role && role.permissions.has(permissionId)) {
        // 检查权限条件
        const permission = this.permissions.get(permissionId);
        if (this.checkConditions(permission.conditions, context)) {
          return true;
        }
      }
    }
    
    return false;
  }
  
  // 检查条件
  checkConditions(conditions, context) {
    return conditions.every(condition => {
      const { field, operator, value } = condition;
      const actualValue = context[field];
      
      switch (operator) {
        case '==':
          return actualValue === value;
        case '!=':
          return actualValue !== value;
        case '>':
          return actualValue > value;
        case '<':
          return actualValue < value;
        case 'in':
          return Array.isArray(value) && value.includes(actualValue);
        default:
          return true;
      }
    });
  }
}

// 使用示例
const permissionModel = new PermissionModel();

// 创建角色
permissionModel.createRole('admin', '管理员');
permissionModel.createRole('editor', '编辑');
permissionModel.createRole('viewer', '查看者');

// 添加权限
permissionModel.addPermission('page:edit', {
  resource: 'page',
  action: 'edit',
  conditions: []
});

permissionModel.addPermission('page:edit:own', {
  resource: 'page',
  action: 'edit',
  conditions: [
    { field: 'pageCreator', operator: '==', value: '${userId}' }
  ]
});

permissionModel.addPermission('page:view', {
  resource: 'page',
  action: 'view',
  conditions: []
});

// 分配权限
permissionModel.assignPermissionToRole('admin', 'page:edit');
permissionModel.assignPermissionToRole('editor', 'page:edit:own');
permissionModel.assignPermissionToRole('editor', 'page:view');
permissionModel.assignPermissionToRole('viewer', 'page:view');

// 分配角色
permissionModel.assignRoleToUser('user-001', 'admin');
permissionModel.assignRoleToUser('user-002', 'editor');
permissionModel.assignRoleToUser('user-003', 'viewer');

// 检查权限
console.log(permissionModel.hasPermission('user-001', 'page:edit'));  // true
console.log(permissionModel.hasPermission('user-002', 'page:edit', { pageCreator: 'user-002' }));  // true
console.log(permissionModel.hasPermission('user-003', 'page:edit'));  // false
```

### 6. 前端权限控制组件

```javascript
// 权限控制组件
const PermissionGuard = ({ user, permission, children, fallback = null }) => {
  const hasPermission = checkPermission(user, permission);
  
  if (hasPermission) {
    return children;
  } else {
    return fallback;
  }
};

// 权限指令
const permissionDirective = {
  mounted(el, binding) {
    const { user, permission } = binding.value;
    const hasPermission = checkPermission(user, permission);
    
    if (!hasPermission) {
      el.style.display = 'none';
    }
  }
};

// 使用示例
<PermissionGuard
  user={currentUser}
  permission={{ resource: 'page', action: 'edit' }}
  fallback={<div>无权限访问</div>}
>
  <Button>编辑页面</Button>
</PermissionGuard>

// 或者使用指令
<button v-permission="{ user: currentUser, permission: { resource: 'page', action: 'edit' } }">
  编辑页面
</button>
```

---